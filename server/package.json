
{
  "name": "buy-brilliance-server",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --watch dist-server/index.js",
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch --loader ts-node/esm index.ts"
  }
}
